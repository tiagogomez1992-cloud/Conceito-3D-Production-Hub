# Copiar para .env antes do primeiro arranque.
POSTGRES_DB=production_hub
POSTGRES_USER=production_hub
# Gerar uma palavra-passe forte, por exemplo: openssl rand -hex 32
POSTGRES_PASSWORD=replace-with-a-long-random-password
# Altera se a porta 80 já estiver em utilização.
HTTP_PORT=80
